AVR USB OneWire

OneWire implementation on AVR with USB connection.

Hardware:
AVR ATTiny2313

Udev:
Make the device accessible by ordinary user:
edit udev-rules/onewire.rules and change GROUPS to match the user groups that should have access
sudo cp udev-rules/onewire.rules /etc/udev/rules.d/
sudo service udev restart
plug in the device again
